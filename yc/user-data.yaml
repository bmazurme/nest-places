users:
  - name: {{ env.YC_VM_USERNAME }}
    groups: sudo
    shell: /bin/bash
    sudo: [ 'ALL=(ALL) NOPASSWD:ALL' ]
    ssh-authorized-keys:
      - {{ env.SSH_KEY }}
runcmd:
  - yc lockbox payload get-text e6qtf1j564cjrn1nkrkq --key config > /data/test.conf
  