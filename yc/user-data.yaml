#cloud-config
# users:
#   - name: {{ env.YC_VM_USERNAME }}
#     groups: sudo
#     shell: /bin/bash
#     sudo: [ 'ALL=(ALL) NOPASSWD:ALL' ]
#     ssh-authorized-keys:
#       - {{ env.cat ~/.ssh/id_rsa.pub }}
users:
  - name: {{ env.YC_VM_USERNAME }}
    groups: [sudo]
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh-authorized-keys:
      - "{{ secrets.SSH_KEY }}"
